name: Build and Deploy AssignmentTracker
on:
  push:
    branches:
      - main
jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - name: Checking out the code
        uses: actions/checkout@v4
      - name: Say Hi to Ganesh
        run: echo "Hello Ganesh"